import { GiftRecord } from "../records/giftRecord"

test('Not inserted GiftRecord should have no ID', () => {
    const newGift = {
        name: 'New gift',
        amount: 2
    }

    console.log(newGift)

})